# 战斗动画业务链 - 快速速查与架构图

## 📊 核心类关系图

```
┌──────────────────────────────────────────────────────────────────┐
│                     游戏战斗管理体系                              │
└──────────────────────────────────────────────────────────────────┘

1️⃣ 阵型管理层
┌─────────────────────────────────┐
│ HorizontalBattleFormationManager │  职责: 角色生成、场景布局、事件管理
├─────────────────────────────────┤
│ - playerSpawnPoints[6]          │  前排左中右，后排左中右
│ - enemySpawnPoints[6]           │
│ - healthBarPrefab               │
│ - formationContainer            │  FormationContainer SO
└──────┬──────────────────────────┘
       │ CreatePlayerCharacter()
       │ CreateEnemyWave()
       ▼
2️⃣ 战斗系统核心层
┌──────────────────────────────────────────────┐
│           AutoBattleAI                        │  职责: 回合制管理、AI决策
├──────────────────────────────────────────────┤
│ - initiativeOrder: List<InitiativeEntry>    │
│ - currentTurnIndex: int                      │
│ - isBattleActive: bool                       │
├──────────────────────────────────────────────┤
│ Public Methods:                              │
│ + StartBattleSequence()                      │
│ + ProcessCurrentTurn()                       │
│ + ExecuteBattleActionEvent()                 │
│ + ProcessAttackHit()                         │
└──────┬──────────────────────────────────────┘
       │ 
       ├─ HorizontalCombatRules (静态类)
       │  ├─ RollAndSortInitiative()          先攻检定
       │  ├─ ResolveAttack()                  命中与伤害
       │  ├─ GetAttackBonus()                 攻击加值
       │  └─ CalculateDamageUnified()         伤害计算
       │
       └─ DND_CharacterAdapter (动画适配器)
          ├─ ExecuteMeleeAttack()             近战流程
          ├─ ExecuteRangedAttack()            远程流程
          ├─ PlayAttackAnimation()            播放攻击
          ├─ PlayHitAnimation()               播放受击
          ├─ PlayDeathAnimation()             播放死亡
          └─ OnSpineEvent()                   Spine事件处理

3️⃣ 角色数据层
┌────────────────────────────────────────┐
│        CharacterStats                  │  职责: 属性、状态、伤害处理
├────────────────────────────────────────┤
│ - currentHitPoints: int                │
│ - statusEffects: List<StatusEffect>    │
│ - template: CharacterTemplate          │
│ - battleSide: BattleSide (P/E)         │
├────────────────────────────────────────┤
│ + ReceiveDamage()                      │
│ + BecomeUnconsciousOrDead()            │
│ + HasStatusEffect()                    │
│ + IsDownOrDead()                       │
└────────────────────────────────────────┘

4️⃣ 动画与表现层
┌──────────────────────────────────────────────┐
│      DND_CharacterAdapter                     │  职责: 动画播放、位移控制
├──────────────────────────────────────────────┤
│ Components:                                  │
│ - skeletonAnimation: SkeletonAnimation       │  Spine运行时
│ - characterStats: CharacterStats             │
│ - animationConfig: CharacterAnimationConfig  │  SO配置
├──────────────────────────────────────────────┤
│ Animation States:                            │
│ - CurrentState: enum CharacterState          │
│ - isAnimating: bool                          │
│ - isForceAttackAnimation: bool               │
│ - currentMoveTween: Tween (DOTween)          │
├──────────────────────────────────────────────┤
│ Events:                                      │
│ + OnAttackHit                                │  攻击命中
│ + OnAnimationComplete                        │  动画完毕
│ + OnStateChanged(string)                     │  状态变化
└──────────────────────────────────────────────┘

5️⃣ 配置资产层
┌────────────────────────────────────────┐
│  CharacterAnimationConfig (SO)         │  映射动画名称和参数
├────────────────────────────────────────┤
│ - idleAnimation: "idle"                │
│ - walkAnimation: "walk"                │
│ - attackAnimation: "attack"            │
│ - hitAnimation: "hit"                  │
│ - deathAnimation: "death"              │
│ - moveSpeed: 2.0                       │
│ - attackDistance: 1.5                  │
│ - moveEase: OutCubic                   │
└────────────────────────────────────────┘

6️⃣ 事件系统层
┌────────────────────────────────────────┐
│  DamageEventChannel_SO                 │  全局伤害事件总线
├────────────────────────────────────────┤
│ Subscribers:                           │
│ - CharacterStats.ReceiveDamage()       │
│ - UI_HealthBar.UpdateDisplay()         │
│ - DamageNumberDisplay.ShowDamage()     │
└────────────────────────────────────────┘
```

---

## 🔄 完整执行流程（超详细版）

### 阶段 1: 战斗初始化 (Initialization Phase)

```csharp
HorizontalBattleFormationManager.Awake()
    └─ HealthBarUIManager.Instance (单例初始化)

AutoBattleAI.Update()
    └─ decisionDelay 定时器触发
        └─ StartBattleSequence() ← 战斗启动入口
            │
            ├─ 1.1 收集参战角色
            │    FindObjectsOfType<CharacterStats>()
            │    └─ allCombatants: List<CharacterStats>
            │
            ├─ 1.2 执行先攻检定 ✨ 关键
            │    HorizontalCombatRules.RollAndSortInitiative(allCombatants)
            │    └─ 循环每个角色:
            │        ├─ dexMod = (dexterity - 10) / 2
            │        ├─ roll = Random.Range(1, 21)
            │        ├─ total = roll + dexMod
            │        └─ 记录 InitiativeEntry(character, total)
            │       按 total 降序排列
            │
            ├─ 1.3 初始化回合数据
            │    initiativeOrder = [...]  (已排序)
            │    currentTurnIndex = 0
            │    isBattleActive = true
            │
            └─ 1.4 开启回合循环
                 StartNextTurn()
                     └─ 设置 turnTimer = 0
```

### 阶段 2: 回合轮询 (Turn Loop Phase)

```csharp
AutoBattleAI.Update() [每帧调用]
    └─ if (isBattleActive && enableAutoBattle)
        └─ if (!isProcessingTurn)
            └─ turnTimer += Time.deltaTime
                └─ if (turnTimer >= decisionDelay)  // 通常 1.0 秒
                    └─ ProcessCurrentTurn() ✨ 回合处理
                        │
                        ├─ 2.1 检查战斗是否结束
                        │     if (IsBattleOver()) EndBattle()
                        │
                        ├─ 2.2 获取当前行动者
                        │     var currentEntry = initiativeOrder[currentTurnIndex]
                        │     var character = currentEntry.character
                        │
                        ├─ 2.3 检查行动能力
                        │     if (!currentEntry.CanAct()) AdvanceToNextTurn()
                        │
                        ├─ 2.4 特殊处理：昏迷状态
                        │     if (character.HasStatusEffect(StatusEffectType.Unconscious))
                        │         character.PerformDeathSaveTick()
                        │
                        ├─ 2.5 AI决策：选择目标
                        │     var chosenAction = DecideBestAction(character)
                        │     └─ action.target: CharacterStats
                        │
                        ├─ 2.6 执行行动 ✨ 核心分发
                        │     ExecuteBattleActionEvent(character, chosenAction, () => {
                        │         currentEntry.MarkAsActed()
                        │         AdvanceToNextTurn()  // 进入下一角色
                        │     })
                        │
                        └─ isProcessingTurn = true
```

### 阶段 3: 行动分发 (Action Dispatch Phase)

```csharp
AutoBattleAI.ExecuteBattleActionEvent(
    CharacterStats attacker,
    BattleAction action,
    System.Action onComplete
)
    │
    ├─ 3.1 获取动画适配器
    │     var attackerAdapter = attacker.GetComponent<DND_CharacterAdapter>()
    │
    ├─ 3.2 判断攻击类型
    │     bool isMeleeAttack = IsCharacterInFrontRow(attacker)
    │     // 前排 = 近战，后排 = 远程
    │
    ├─ 3.3 分支: 近战 vs 远程
    │  ├─ if (isMeleeAttack)
    │  │   attackerAdapter.ExecuteMeleeAttack(
    │  │       target.transform,
    │  │       onAttackHit: () => ProcessAttackHit(...),
    │  │       onComplete: () => onComplete.Invoke()
    │  │   )
    │  │
    │  └─ else
    │      attackerAdapter.ExecuteRangedAttack(
    │          target.transform,
    │          onAttackHit: () => ProcessAttackHit(...),
    │          onComplete: () => onComplete.Invoke()
    │      )
    │
    └─ 3.4 记录行动日志
         GameLog.LogAction(attacker, "对" + target + "发动攻击")
```

### 阶段 4a: 近战攻击流程 (Melee Attack Phase)

```csharp
DND_CharacterAdapter.ExecuteMeleeAttack(
    Transform target,
    System.Action onAttackHit,
    System.Action onComplete
)
    │
    ├─ 4a.1 状态检查
    │  ├─ if (IsTerminalState()) return           // 死亡/昏迷禁止
    │  ├─ if (isAnimating) return                 // 正在动画中禁止
    │  └─ isAnimating = true                      // 上锁
    │
    ├─ 4a.2 计算攻击位置
    │     Vector3 attackPosition = CalculateAttackPosition(target)
    │     // = target.position - direction * animationConfig.attackDistance
    │     // 通常距离目标 1.5 单位
    │
    ├─ 4a.3 开始移动阶段
    │     PlayWalkAnimation()
    │     // SkeletonAnimation.SetAnimation("walk", loop: true)
    │
    ├─ 4a.4 计算移动时长
    │     float moveDistance = Vector3.Distance(position, attackPosition)
    │     float moveDuration = moveDistance / animationConfig.moveSpeed
    │     // 速度通常 2.0 单位/秒
    │
    ├─ 4a.5 执行移动 Tween
    │     currentMoveTween = transform.DOMove(attackPosition, moveDuration)
    │         .SetEase(animationConfig.moveEase)  // OutCubic
    │         .OnUpdate(() => {
    │             // 检查是否已接近目标
    │             if (distance <= 0.1f) {
    │                 transform.position = attackPosition
    │                 currentMoveTween.Kill()
    │                 
    │                 // ↓ 切换到攻击阶段
    │                 isForceAttackAnimation = true  // 上攻击锁
    │                 CutImmediatelyForAttack()       // 清轨+复位
    │                 ExecuteAttackAtPosition(target, onAttackHit, () => {
    │                     // 攻击完成，返回原位
    │                     currentMoveTween = transform.DOMove(originalPosition, moveDuration)
    │                         .OnComplete(() => {
    │                             isAnimating = false
    │                             PlayIdleAnimation()
    │                             onComplete.Invoke()
    │                         })
    │                 })
    │             }
    │         })
    │         .OnComplete(() => {
    │             // 或通过 OnComplete 回调完成（备用）
    │             if (!arrivalTriggered) {
    │                 proceedAfterArrive()
    │             }
    │         })
    │
    └─ [等待 Tween 完成] → 下一阶段
```

### 阶段 4b: 远程攻击流程 (Ranged Attack Phase)

```csharp
DND_CharacterAdapter.ExecuteRangedAttack(
    Transform target,
    System.Action onAttackHit,
    System.Action onComplete
)
    │
    ├─ 4b.1 状态检查（同近战）
    │     if (IsTerminalState() || isAnimating) return
    │     isAnimating = true
    │
    ├─ 4b.2 直接执行攻击（无位移）
    │     ExecuteAttackAtPosition(target, onAttackHit, () => {
    │         isAnimating = false
    │         onComplete.Invoke()  // 无需返回原位
    │     })
    │
    └─ [等待动画完成] → 下一阶段
```

### 阶段 5: 统一的攻击执行 (Unified Attack Execution Phase) ✨

```csharp
DND_CharacterAdapter.ExecuteAttackAtPosition(
    Transform target,
    System.Action onAttackHit,
    System.Action onComplete
)
    │
    ├─ 5.1 预处理
    │     isForceAttackAnimation = true      // 上锁（禁止Walk/Idle覆盖）
    │     CutImmediatelyForAttack()          // 清所有轨道 + 复位骨架
    │         └─ AnimationState.ClearTracks()
    │         └─ Skeleton.SetToSetupPose()
    │         └─ AnimationState.Apply() + UpdateWorldTransform()
    │
    ├─ 5.2 播放攻击动画
    │     PlayAttackAnimation()
    │         └─ string attackAnimName = FindBestAttackAnimationName()
    │         └─ PlayAnimation(attackAnimName, loop: false)
    │             └─ SkeletonAnimation.SetAnimation(attackAnimName, false)
    │             └─ AnimationState.Apply()
    │             └─ Update(0f)  // 立即刷新渲染
    │
    ├─ 5.3 获取动画时长
    │     float attackAnimationDuration = GetAnimationDuration(attackAnimName)
    │     // 读取Spine动画的实际时长
    │
    ├─ 5.4 设置攻击命中回调
    │     DOVirtual.DelayedCall(visualDelay: 0.03f, () => {
    │         // 3. 设置备用触发器（防止Spine事件丢失）
    │         DOVirtual.DelayedCall(
    │             attackAnimationDuration * 0.5f,  // 通常动画50%处
    │             () => {
    │                 if (!hasAttackHitTriggered) {
    │                     hasAttackHitTriggered = true
    │                     onAttackHit.Invoke()  // ← 触发伤害结算
    │                 }
    │             }
    │         )
    │     })
    │
    ├─ 5.5 绑定Spine事件监听
    │     OnAttackHit += tempAttackHitCallback = () => {
    │         if (!hasAttackHitTriggered) {
    │             hasAttackHitTriggered = true
    │             onAttackHit.Invoke()  // ← 触发伤害结算（主路径）
    │         }
    │     }
    │
    ├─ 5.6 设置攻击完成回调
    │     tempAnimCompleteCallback = () => {
    │         if (!hasAttackCompleteTriggered) {
    │             hasAttackCompleteTriggered = true
    │             isForceAttackAnimation = false  // 解除锁
    │             onComplete.Invoke()  // ← 触发后续流程
    │         }
    │     }
    │     OnAnimationComplete += tempAnimCompleteCallback
    │
    ├─ 5.7 备用完成触发器
    │     DOVirtual.DelayedCall(
    │         attackAnimationDuration + 0.1f,
    │         () => {
    │             if (!hasAttackCompleteTriggered) {
    │                 tempAnimCompleteCallback.Invoke()
    │             }
    │         }
    │     )
    │
    └─ [等待Spine事件或定时器触发] → 5.8 & 5.9
```

### 阶段 5.8: 攻击命中 (Attack Hit Callback)

```
【主路径】 Spine事件触发
    OnSpineEvent(TrackEntry, Spine.Event e)
        └─ if (eventName.Contains("atk") || "attack" || "damage")
            └─ OnAttackHit.Invoke()
                └─ ProcessAttackHit(...) ✨ 伤害结算

【备用路径】 定时器触发 (防丢失)
    DOVirtual.DelayedCall(animDuration * 0.5f)
        └─ if (!hasAttackHitTriggered)
            └─ ProcessAttackHit(...) ✨ 伤害结算
```

### 阶段 6: 伤害结算 (Damage Resolution Phase) ✨

```csharp
AutoBattleAI.ProcessAttackHit(
    CharacterStats attacker,
    CharacterStats target,
    bool isMeleeAttack
)
    │
    ├─ 6.1 检查目标特殊状态
    │     int advantageFlag = 0
    │     if (target.HasStatusEffect(StatusEffectType.Unconscious)) {
    │         advantageFlag = isMeleeAttack ? 1 : -1
    │         // 近战优势掷骰，远程劣势掷骰
    │     }
    │
    ├─ 6.2 调用战斗规则系统 ✨ 核心掷骰
    │     var attackResult = HorizontalCombatRules.ResolveAttack(
    │         attacker,
    │         target,
    │         advantageFlag,
    │         isMeleeAttack
    │     )
    │
    └─ 6.3 根据结果处理伤害
         if (attackResult.isHit) {
             int damage = attackResult.damage
             bool isCritical = attackResult.isCritical
             target.ReceiveDamage(damage, isCritical)
         }
```

### 阶段 6.2: 攻击结算规则 (Attack Resolution Rules)

```csharp
HorizontalCombatRules.ResolveAttack(
    CharacterStats attacker,
    CharacterStats target,
    int advantageFlag,
    bool isMeleeAttack
)
    │
    ├─ 6.2.1 确定攻击属性
    │     bool isSpell = attacker.template.defaultAttackType == Spell
    │     var weapon = isSpell ? null : FindEquippedWeapon(attacker)
    │
    ├─ 6.2.2 计算攻击加值
    │     int attackBonus = GetAttackBonus(attacker, isMeleeAttack, weapon)
    │     // 基于武器+属性修正+熟练加值
    │
    ├─ 6.2.3 执行掷骰 ✨ 关键！
    │     int roll1 = Random.Range(1, 21)         // d20
    │     int roll2 = Random.Range(1, 21)         // 若有优势/劣势
    │     int d20 = (advantageFlag > 0)
    │               ? Mathf.Max(roll1, roll2)     // 优势: 取最大
    │               : (advantageFlag < 0)
    │                 ? Mathf.Min(roll1, roll2)   // 劣势: 取最小
    │                 : roll1                      // 正常: 单骰
    │
    ├─ 6.2.4 暴击判定
    │     bool isCritical = (d20 == 20)
    │
    ├─ 6.2.5 计算总攻击值
    │     int totalAttack = d20 + attackBonus
    │
    ├─ 6.2.6 获取目标AC
    │     int targetAc = target.CurrentArmorClass
    │     // 基于护甲类型 + 敏捷修正 + 盾牌
    │
    ├─ 6.2.7 命中判定 ✨
    │     bool isHit = isCritical || totalAttack >= targetAc
    │
    ├─ 6.2.8 伤害计算（如果命中）
    │     if (isHit) {
    │         int damage = CalculateDamageUnified(
    │             attacker,
    │             isCritical,
    │             isSpell,
    │             hitAbility,
    │             weapon
    │         )
    │         // 骰子投掷: 
    │         // - 暴击时双倍骰子数
    │         // - 加上能力修正
    │         // - 应用抗性/易伤
    │     }
    │
    ├─ 6.2.9 记录日志
    │     GameLog.LogHit(attacker, target, ...)
    │     GameLog.LogDamage(attacker, target, damage, ...)
    │
    └─ 6.2.10 返回结果
         return new AttackResult {
             isHit: true/false,
             damage: intValue,
             isCritical: bool,
             damageType: DamageType,
             description: string
         }
```

### 阶段 5.9: 攻击动画完成 (Attack Animation Complete)

```
【主路径】 Spine动画完成事件
    OnSpineAnimationComplete(TrackEntry trackEntry)
        └─ if (trackEntry.Animation.Name == "attack")
            └─ OnAnimationComplete.Invoke()
                └─ ExecuteAttackAtPosition() 的 onComplete 回调

【备用路径】 定时器触发
    DOVirtual.DelayedCall(animDuration + 0.1f)
        └─ if (!hasAttackCompleteTriggered)
            └─ tempAnimCompleteCallback.Invoke()
```

### 阶段 7: 角色受击反应 (Character Hit Reaction)

```csharp
CharacterStats.ReceiveDamage(int damageAmount, bool isCritical)
    │
    ├─ 7.1 扣除生命值
    │     currentHitPoints -= damageAmount
    │
    ├─ 7.2 发布伤害事件
    │     var channel = damageEventChannel
    │     if (channel != null) {
    │         var info = new DamageInfo(this, attacker, damage, isCritical)
    │         channel.RaiseEvent(info)  // ← 事件总线
    │     }
    │
    ├─ 7.3 播放受击动画
    │     var adapter = GetComponent<DND_CharacterAdapter>()
    │     adapter.PlayHitAnimation()
    │         └─ PlayAnimation("hit", loop: false)
    │
    ├─ 7.4 更新UI（通过事件订阅）
    │     // HealthBarUIManager 监听伤害事件
    │     // DamageNumberDisplay 显示伤害数字
    │
    ├─ 7.5 检查是否死亡
    │     if (currentHitPoints <= 0) {
    │         BecomeUnconsciousOrDead()
    │     }
    │
    └─ [等待受击动画完成]
```

### 阶段 8: 死亡处理 (Death Handling)

```csharp
CharacterStats.BecomeUnconsciousOrDead()
    │
    ├─ 8.1 添加昏迷状态
    │     statusEffects.Add(StatusEffectType.Unconscious)
    │
    ├─ 8.2 播放昏迷/死亡动画
    │     var adapter = GetComponent<DND_CharacterAdapter>()
    │     adapter.PlayDeathAnimation()  或  PlayUnconsciousAnimation()
    │         ├─ ClearTracks()
    │         ├─ SetToSetupPose()
    │         └─ SetAnimation("death" 或 "unconscious", loop: false)
    │
    ├─ 8.3 标记已播放死亡动画
    │     HasPlayedDeath = true
    │
    ├─ 8.4 从战斗中移除
    │     // HorizontalBattleFormationManager 或 AutoBattleAI
    │     // 调用 RemoveCharacterFromBattle(character)
    │
    └─ [角色终止，无法继续行动]
```

### 阶段 9: 回合结束与流转 (Turn Completion & Transition)

```csharp
[回到 ExecuteMeleeAttack 的 onComplete 回调]
    │
    ├─ 9.1 返回原位（仅近战）
    │     currentMoveTween = transform.DOMove(originalPosition, duration)
    │         .OnComplete(() => {
    │             isAnimating = false
    │             PlayIdleAnimation()
    │         })
    │
    ├─ 9.2 回调链返回 AutoBattleAI
    │     onComplete.Invoke()
    │         └─ ProcessCurrentTurn() 中的回调:
    │             ├─ currentEntry.MarkAsActed()  // 标记已行动
    │             └─ AdvanceToNextTurn()         // 进入下一角色
    │
    ├─ 9.3 推进回合索引
    │     currentTurnIndex++
    │     if (currentTurnIndex >= initiativeOrder.Count) {
    │         currentTurnIndex = 0  // 循环回第一个
    │     }
    │
    ├─ 9.4 重置回合数据
    │     isProcessingTurn = false
    │     turnTimer = 0f
    │
    ├─ 9.5 检查战斗是否结束
    │     if (IsBattleOver()) {
    │         EndBattle()
    │     }
    │
    └─ 9.6 回到下一个角色的回合
         [返回 ProcessCurrentTurn() 处理下一个角色]
```

---

## 🎯 关键方法速查表

| 方法 | 所在类 | 职责 | 输入 | 输出 |
|-----|--------|------|------|------|
| `StartBattleSequence()` | AutoBattleAI | 战斗启动、先攻检定 | - | initiativeOrder |
| `RollAndSortInitiative()` | HorizontalCombatRules | 先攻掷骰与排序 | List<CharacterStats> | List<InitiativeEntry> |
| `ProcessCurrentTurn()` | AutoBattleAI | 单个角色回合处理 | - | void |
| `DecideBestAction()` | AutoBattleAI | AI决策选择目标 | CharacterStats | BattleAction |
| `ExecuteBattleActionEvent()` | AutoBattleAI | 行动分发 | attacker, action, callback | void |
| `ExecuteMeleeAttack()` | DND_CharacterAdapter | 近战攻击（含位移） | target, hitCallback, completeCallback | void (DOTween控制) |
| `ExecuteRangedAttack()` | DND_CharacterAdapter | 远程攻击（原地） | target, hitCallback, completeCallback | void (DOTween控制) |
| `ExecuteAttackAtPosition()` | DND_CharacterAdapter | 统一的攻击执行 | target, hitCallback, completeCallback | void (Spine事件驱动) |
| `PlayAttackAnimation()` | DND_CharacterAdapter | 播放攻击动画 | - | void |
| `PlayAnimation()` | DND_CharacterAdapter | 基础动画播放 | animationName, loop | void |
| `ResolveAttack()` | HorizontalCombatRules | 掷骰与伤害结算 | attacker, target, advantageFlag | AttackResult |
| `ProcessAttackHit()` | AutoBattleAI | 伤害处理分发 | attacker, target, isMelee | void |
| `ReceiveDamage()` | CharacterStats | 受伤处理 | damageAmount, isCritical | void |
| `PlayHitAnimation()` | DND_CharacterAdapter | 播放受击动画 | - | void |
| `PlayDeathAnimation()` | DND_CharacterAdapter | 播放死亡动画 | - | void |
| `OnSpineEvent()` | DND_CharacterAdapter | Spine事件处理 | trackEntry, event | void |
| `OnSpineAnimationComplete()` | DND_CharacterAdapter | 动画完成处理 | trackEntry | void |

---

## 📈 时间序列全景图

```
[时间线] (单位: 秒)

00.00 ──┬─ StartBattleSequence() 
        │  ├─ RollAndSortInitiative()
        │  └─ 先攻检定完成: [A角, B角, C敌1, C敌2, ...]
        │
01.00 ──┼─ ProcessCurrentTurn() → A角 的回合
        │
01.10 ──┼─ DecideBestAction(A) → 选择目标 (C敌1)
        │
01.15 ──┼─ ExecuteBattleActionEvent()
        │  └─ IsCharacterInFrontRow(A) → true (近战)
        │
01.20 ──┼─ ExecuteMeleeAttack() 开始
        │  ├─ PlayWalkAnimation()
        │  └─ DOMove(攻击位置, duration=0.3s)
        │
01.50 ──┼─ 移动完成 (arriveSnapThreshold 触发)
        │  ├─ CutImmediatelyForAttack()
        │  └─ ExecuteAttackAtPosition() 开始
        │
01.52 ──┼─ PlayAttackAnimation()
        │  └─ SetAnimation("attack", loop=false)
        │
01.65 ──┼─ OnSpineEvent: "attack_hit" 触发 ✓
        │  └─ ProcessAttackHit(A, C敌1, true)
        │     ├─ ResolveAttack():
        │     │  ├─ d20 = 15, 攻击加值 = 3, 总计 = 18 vs AC 14 → 命中!
        │     │  ├─ 伤害骰: 1d8 = 6, 力量修正 = +2 → 8 点伤害
        │     │  └─ 暴击? d20==20? false
        │     │
        │     └─ C敌1.ReceiveDamage(8, false)
        │        ├─ currentHitPoints: 20 → 12
        │        ├─ RaiseEvent(DamageInfo)
        │        └─ PlayHitAnimation()
        │
01.75 ──┼─ OnSpineAnimationComplete: "attack" 触发
        │  └─ ExecuteAttackAtPosition() 的 onComplete 回调
        │     ├─ 返回原位的 DOMove(originalPosition, duration=0.3s) 开始
        │     └─ PlayWalkAnimation()
        │
02.05 ──┼─ 返回原位完成
        │  ├─ PlayIdleAnimation()
        │  └─ onComplete.Invoke()
        │
02.10 ──┼─ AdvanceToNextTurn()
        │  ├─ currentTurnIndex++
        │  ├─ isProcessingTurn = false
        │  └─ turnTimer = 0
        │
03.10 ──┼─ ProcessCurrentTurn() → B角 的回合
        │  └─ ... (同样流程)
        │
... ...多个角色轮流行动...
        │
NN.NN ──┼─ IsBattleOver() → true
        │  └─ EndBattle()
        │     ├─ 计算战斗奖励
        │     ├─ 标记胜负
        │     └─ 清理UI
```

---

## 🔧 配置检查单

```yaml
前置条件检查:
  战斗场景:
    ✓ HorizontalBattleFormationManager 已放置
    ✓ AutoBattleAI 已放置
    ✓ 玩家角色Prefab已配置
    ✓ 敌人浪次已配置
  
  角色Prefab配置:
    ✓ CharacterStats 组件
    ✓ SkeletonAnimation (Spine组件)
    ✓ DND_CharacterAdapter 组件
    ✓ CharacterEquipment 组件 (可选)
    ✓ CharacterTemplate SO引用
  
  动画配置:
    ✓ CharacterAnimationConfig SO已创建
    ✓ 每个角色都有对应的 SO
    ✓ 动画名称与Spine完全一致
    ✓ moveSpeed, attackDistance 已配置
  
  事件系统:
    ✓ DamageEventChannel_SO 已创建
    ✓ HorizontalBattleFormationManager.globalDamageEventChannel 已赋值
    ✓ 或 CharacterStats.damageEventChannel 已赋值
  
  Spine配置:
    ✓ Skeleton Data Asset 已导入
    ✓ 动画列表: idle, walk, attack, hit, death 已定义
    ✓ attack 动画中有事件 "attack_hit" (或支持自动识别)
    ✓ 各动画时长合理 (attack 通常 0.4~0.8s)
```

---

## 🐛 常见卡点调试

### 攻击动画不播放
```csharp
// 检查点
[1] animationConfig 是否为空?
    → DND_CharacterAdapter Inspector 检查 animationConfig 字段

[2] 动画名是否匹配?
    → 对比 animationConfig.attackAnimation 与 Spine 中的动画名
    → Debug.Log(skeletonAnimation.Skeleton.Data.Animations.Items[i].Name)

[3] PlayAnimation() 是否真的被调用?
    → 在 PlayAttackAnimation() 添加日志
    → 检查 isForceAttackAnimation 是否已上锁

[4] SetAnimation() 是否有异常?
    → 在 try-catch 中打印 ex.Message
```

### 位移Tween未完成就开始攻击
```csharp
// 调整阈值
const float arriveSnapThreshold = 0.1f;  // 改为 0.05f 或 0.15f

// 或调整速度
animationConfig.moveSpeed = 1.5f;  // 降速
animationConfig.moveEase = DG.Tweening.Ease.Linear;  // 改为线性
```

### 伤害事件未触发
```csharp
// 检查点
[1] DamageEventChannel 是否赋值?
    → 查看 HorizontalBattleFormationManager.globalDamageEventChannel

[2] RaiseEvent() 是否被调用?
    → 在 HorizontalCombatRules.ResolveAttack() 结尾检查

[3] 事件是否有订阅者?
    → CharacterStats.damageEventChannel != null?
    → HealthBarUIManager 是否注册?
```

### 死亡动画被打断
```csharp
// 在 PlayDeathAnimation() 中确保清轨
skeletonAnimation.AnimationState.ClearTracks();
currentMoveTween?.Kill();
isForceAttackAnimation = false;

// 检查 IsTerminalState() 是否正确
return characterStats.IsDownOrDead() || characterStats.HasStatusEffect(StatusEffectType.Unconscious);
```


